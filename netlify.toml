[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/chamcong run build"
  publish = "artifacts/chamcong/dist/public"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"
  NODE_ENV = "production"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
